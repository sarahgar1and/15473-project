{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Renderer",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/3D-OpenGL",
            "args": [],
            "cwd": "${workspaceFolder}",
            "MIMode": "lldb",
            "preLaunchTask": "build"
        }
    ]
}